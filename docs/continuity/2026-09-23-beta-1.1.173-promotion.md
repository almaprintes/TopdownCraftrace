# Promoción BETA 1.1.173

La rama `beta-1.0` adopta exclusivamente el árbol verificado de `main` DEV
1.1.173 y cambia su identidad visible a `BETA 1.1.173`. No se recupera código de
la BETA 1.1.171 ni de ramas auxiliares.

Esta candidata incluye:

- recuperación canónica del x2 postcarrera desde `RaceSessionRewardsScene`;
- los seis accesos rewarded verificados e idempotentes;
- ignición real CC0 desde el botón común `◉ ARRANCAR MOTOR`;
- continuidad del motor propio de cada coche tras el crossfade;
- build PROD sin herramientas Admin y con Judge Mode conservado.

La identidad Android asociada es `versionName 1.1.173`, `versionCode 4`, package
`com.craftracestudio.topdownrace` y la misma firma de subida de las betas
anteriores.
