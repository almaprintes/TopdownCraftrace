// Tester exchange remains a guarded module for deployment compatibility.
// Lap sharing now lives inside the real replay viewer in StatsTesterExchangeScene.
// The old record-row share-arrow decoration and MutationObserver are intentionally removed.
export {};
