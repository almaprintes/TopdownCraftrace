name: Build and publish Pages (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Standalone preview active
        run: echo "GitHub Pages is currently served directly from main/index.html. The Vite rebuild workflow is intentionally disabled while the full source migration is repaired."
